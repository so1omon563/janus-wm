# Janus Architecture

This directory records the system shape behind Janus.

Architecture documents should explain how implementation choices support the
product design. They should stay separate from design RFCs because technical
constraints and system boundaries evolve differently from product intent.

Use `adr/` for architectural decisions that should remain easy to find later.
